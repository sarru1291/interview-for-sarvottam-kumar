export const base_url = "https://api.spacexdata.com";
export const version = "v4";

// Endpoints
export const all_launches_ep = `${base_url}/${version}/launches`;
export const all_launchpads_ep = `${base_url}/${version}/launchpads`;
export const all_rockets_ep = `${base_url}/${version}/rockets`;
export const all_payloads_ep = `${base_url}/${version}/payloads`;
